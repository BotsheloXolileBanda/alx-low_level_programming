#include <stdio.h>

/**
 * main - Prints numbers from 0 to 10.
 *
 * Return: A value of 0.
 */
int main(void)
{
	int g = 48;

	while (g <= 57)
	{
		putchar(g);
		g++;
	}
	putchar('\n');
	return (0);
}
