#include "main.h"

/**
  * main - Prints the number of arguments it has.
  *
  * @argc: The argument count.
  * @argv: THe argument strings.
  * Return: 0 for success and otherwise failure.
  */
int main(int argc, char *argv[])
{
	(void)argv;

	printf("%d\n", argc);
	return (0);
}
