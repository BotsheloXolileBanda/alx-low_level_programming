#include "main.h"

/**
  * main - Prints the executable name.
  *
  * @argc: The argument count variable.
  * @argv: The argument strings.
  * Return: 0 for success and otherwise failure.
  */
int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);

	return (0);
}
